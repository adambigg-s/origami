pub const collect = @import("collect.zig");
pub const Collector = collect.Collector;
pub const iterator = @import("iterator.zig");
pub const Iterator = iterator.Iterator;
pub const IteratorMut = iterator.IteratorMut;

test "basic iterator" {}

test "foreach iterator" {}

test "map iterator" {}

test "fold iterator" {}

test "collect iterator" {}
